spring:
  application:
    name: config-admin
    
server:
  port: ${port:8090}
  
logging:
  level:
    org.springframework.cloud: 'DEBUG'
  file: /opt/log/${ctrip.appid}/config-admin.log

ctrip:
  appid: 100003172
